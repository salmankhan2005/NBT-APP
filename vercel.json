{
  "version": 2,
  "buildCommand": "cd \"admin app\" && npm install && npx expo export --platform web",
  "outputDirectory": "admin app/dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
